package com.example.Pin_inator.Models.Core;

public class Enum {
    public enum PinType {
        Food,
        PointOfInterest,
        ViewPoint,
        Mark,
        Custom,
        Stay,
        Cautious
    }
    public enum BlockType {
        TEXT,
        IMAGE
    }
}
